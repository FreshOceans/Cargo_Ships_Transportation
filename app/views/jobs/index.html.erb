<p id="notice"><%= notice %></p>

<h1>Jobs Index</h1>

<%= link_to 'New Job', new_user_job_path(current_user.id) %>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @jobs.each do |job| %>
      <tr>
          <td></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
